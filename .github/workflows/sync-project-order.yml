name: Sync Project Order

on:
  schedule:
    # Daily at 08:00 UTC. Only a safety net - a real change arrives through
    # repository_dispatch within seconds, so the second sweep never had anything to find.
    - cron: '0 8 * * *'
  repository_dispatch:
    types: [project-deployed]
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write

concurrency:
  group: sync-project-order
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '20'

      - name: Sync project dates from GitHub
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: node landing-page/scripts/sync-repos.js

      - name: Check for changes
        id: changes
        run: |
          git diff --quiet landing-page/projects.json landing-page/repos.json || echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Commit and push
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add landing-page/projects.json landing-page/repos.json
          git commit -m "chore: sync project order from GitHub activity"
          git pull --rebase
          git push
